# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.marimo/

# Local Colab/WSL test scratch (standalone uv, venvs, caches)
.colab-test/

# Data — raw/processed never committed (see docs/setup-steps.md step 8).
# Small *derived* artifacts (data/derived/) ARE committed, so no blanket *.parquet rule.
data/raw/
data/processed/

# Trained artifacts
models/*.pkl
models/*.joblib

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Agent tooling artifacts (not project code)
.agents/
